# Phase 03: Message Formatting

## Objective
Tạo dòng text hiển thị cho mỗi trạm MLL đã được gom nhóm.

## Implementation Steps
1. [ ] Duyệt qua `mll_groups`.
2. [ ] Format danh sách network thành chuỗi như `[3G, 4G]`.
3. [ ] Append dòng `  Label [Nets] - Time` vào danh sách `lines`.

## Files to Modify
- `smartw/worker.py`
