# Phase 01: Data Verification

## Objective
Xác nhận cấu trúc dữ liệu của MLL alarms để đảm bảo trường `network` tồn tại và có giá trị chính xác.

## Implementation Steps
1. [ ] Đọc file `data/smartw/mll.json` (nếu có) để xem sample data.
2. [ ] Kiểm tra hàm `_norm_net` trong `worker.py` để đảm bảo hỗ trợ các giá trị network từ MLL.

## Files to Modify
- N/A (Read-only phase)
