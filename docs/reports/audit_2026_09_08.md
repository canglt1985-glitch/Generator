# Báo Cáo Audit & Dọn Dẹp Code Rác - 08/09/2026

## 🏥 Tổng quan sức khỏe dự án
- 🔴 **Critical / Rác phát sinh:** 5 mục (File/Thư mục rác tạo nhầm hoặc file tạm dung lượng lớn)
- 🟡 **Warnings / Code Smell:** 3 mục (File script test tạm sót lại, file duplicate)
- 🟢 **Suggestions / Tối ưu:** Dọn dẹp `console.log` và cache thừa

---

## 🔴 1. Critical - Code rác & File tạm cần xóa ngay
1. **Thư mục `smartw/` ở thư mục gốc:**
   - **Vấn đề:** Thư mục Node.js độc lập vừa khởi tạo nhầm (gồm `BRIEF.md`, `ROADMAP.md`, `package.json`, `src/`). Dữ liệu thực tế của SmartW đã nằm trong `backend/smartw_worker.py`.
   - **Hậu quả:** Gây rối cấu trúc codebase, nhầm lẫn cho dev/AI sau này.
   - **Hướng xử lý:** Xóa toàn bộ thư mục `smartw/`.

2. **File Zip Backup dung lượng lớn:**
   - `backup_tvt3_v2_before_refactor.zip` (~1.7 MB) nằm ở root directory.
   - **Hậu quả:** Làm nặng git repository và workspace.
   - **Hướng xử lý:** Xóa file zip tạm.

3. **Các file Script / Text tạm ở gốc workspace:**
   - `read_excel.py`, `read_excel.txt`: Script đọc Excel tạm.
   - `output_test_26DNa245.docx`: File Word export test tạm.
   - `smart_trade_link`: File mốc tạm 38 bytes.
   - **Hướng xử lý:** Xóa khỏi workspace.

---

## 🟡 2. Warnings - File rác sót lại trong backend
1. **Script test rác trong `web-app/`:**
   - File `web-app/scratch_test_label.py`: File test tạm khi soi label.
2. **File duplicate requirements:**
   - `web-app/requirements_utf8.txt` trùng thông tin với `requirements.txt`.

---

## 🟢 3. Suggestions - Tối ưu & Dọn dẹp Frontend
1. Các câu lệnh `console.log` debug tạm trong `tvt3_v2/src` có thể dọn dẹp bớt để console gọn gàng hơn.

---

## 📋 Đề xuất xử lý (Action Plan)
Có thể kích hoạt chế độ **5️⃣ 🔧 FIX ALL** để tự động dọn dẹp sạch toàn bộ các file rác trên.
